<UserControl x:Class="Forum.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Forum.Views"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920">
    <UserControl.Resources>
        <Style x:Key="myHeaderStyle" TargetType="{x:Type GridViewColumnHeader}">
            <Setter Property="Visibility" Value="Visible"/>
        </Style>
    </UserControl.Resources>
    <Grid Background="#FF2D2D2D">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="1" Margin="30">
                <Button HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontFamily="Comic Sans MS" Command="{Binding ClickNewTopic}">
                    <Viewbox Stretch="Uniform">
                        <TextBlock Text="+" FontSize="80" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Viewbox>
                </Button>
            </Border>
        </Grid>
        <ListBox HorizontalContentAlignment="Stretch" Grid.Row="1" d:ItemsSource="{d:SampleData ItemCount=5}" ItemsSource="{Binding Topics}" Name="TopicList" Background="#FF2D2D2D">
            <ListBox.Resources>
                <Style TargetType="Button">
                    <Style.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                            <Setter Property="BorderThickness" Value="0"/>
                        </Style>
                    </Style.Resources>
                </Style>
                <Style TargetType="TextBlock">
                    <Style.Resources>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#FFBBBBBB"/>
                        </Style>
                    </Style.Resources>
                </Style>
            </ListBox.Resources>
            <ListBox.ItemTemplate>

                <DataTemplate>
                    <Border CornerRadius="5" Margin="100,10" BorderBrush="#FF272727" BorderThickness="2">
                        <Button Command="{Binding DataContext.SelectTopic, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Height="100" Background="#FF3C3C3C">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="{Binding Title}" Grid.Column="0" Grid.Row="0" HorizontalAlignment="Center" Foreground="#FFBBBBBB"/>
                                <TextBlock Text="{Binding Description}" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Center" Foreground="#FFBBBBBB"/>
                            </Grid>
                            

                        </Button>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</UserControl>
