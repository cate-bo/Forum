<UserControl x:Class="Forum.Views.ThreadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Forum.Views"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920">
    <Grid Name="grd_main">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="80"/>
            <RowDefinition/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding _thread.Title}" FontSize="40"/>
        <Button Name="FollowButton" Content="follow" HorizontalAlignment="Right" Margin="15" VerticalAlignment="Top"/>
        <TextBlock Grid.Row="1" HorizontalAlignment="Left" TextWrapping="Wrap" Text="{Binding OPsUsername}" VerticalAlignment="Top" Margin="15"/>
        <TextBlock Grid.Row="1" HorizontalAlignment="Center" Margin="0" TextWrapping="Wrap" Text="{Binding OP.Text}" VerticalAlignment="Center"/>
        <TreeView Grid.Row="2" Name="PostList" Margin="10"/>
        <Button Content="post" HorizontalAlignment="Right" Margin="20" Grid.Row="3" VerticalAlignment="Bottom" Command="{Binding ClickPostButton}"/>
        <TextBox HorizontalAlignment="Stretch" Margin="20,20,20,40" Grid.Row="3" TextWrapping="Wrap" Text="{Binding ReplyText}" VerticalAlignment="Stretch"/>


    </Grid>
</UserControl>
